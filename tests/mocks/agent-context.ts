import { project } from '@/tests/mocks/project'
import { user } from '@/tests/mocks/user'
import { AgentContext } from '@/lib/types'

const objective = {
  userId: user.id,
  id: '4biC6Ld',
  createdAt: new Date(0),
  outcome: 'Become the go-to brand for trendy, meme-inspired crypto T-shirts',
  interval: 'month',
  progress: 0,
  updatedAt: new Date(0),
  projectId: project.id,
  labelId: '',
  lastRunAt: new Date(0),
  parentId: '',
  cycleId: '',
}

// TODO: Add resources and applications
export const agentContext: AgentContext = {
  project,
  run: {
    userId: user.id,
    id: 'DkiC6LG',
    createdAt: new Date(0),
    updatedAt: new Date(0),
    completedAt: new Date(0),
    projectId: project.id,
    objectiveId: objective.id,
    inngestIds: [],
    status: 'pending',
    tasks: [],
    outputUrl: '',
    sharePath: '',
  },
  billingAccount: {
    subscriptionUsageId: 'system',
    createdAt: new Date(0),
    updatedAt: new Date(0),
    id: 'system',
    userId: user.id,
    approved: true,
    stripeCustomerId: null,
    stripeSubscriptionId: null,
    private: false,
    appIds: [],
  },
  objective,
  keyResults: [
    {
      userId: user.id,
      id: 'ggcDevF',
      createdAt: new Date(0),
      updatedAt: new Date(0),
      type: 'numeric',
      value: 0.0,
      objectiveId: objective.id,
      name: 'Website conversion rate',
      projectId: project.id,
    },
    {
      userId: user.id,
      id: 'GrhlCNJ',
      createdAt: new Date(0),
      updatedAt: new Date(0),
      type: 'numeric',
      value: 0.0,
      objectiveId: objective.id,
      name: 'Unique meme T-shirt designs',
      projectId: project.id,
    },
    {
      userId: user.id,
      id: '7jdjXlP',
      createdAt: new Date(0),
      updatedAt: new Date(0),
      type: 'numeric',
      value: 0.0,
      objectiveId: objective.id,
      name: 'Social media followers',
      projectId: project.id,
    },
    {
      userId: user.id,
      id: '2dKGxkz',
      createdAt: new Date(0),
      updatedAt: new Date(0),
      type: 'numeric',
      value: 0.0,
      objectiveId: objective.id,
      name: 'Monthly revenue',
      projectId: project.id,
    },
  ],
  sentinel: false,
  taskStep: 0,
}
